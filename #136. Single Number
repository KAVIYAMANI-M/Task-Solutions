class Solution {
    public int singleNumber(int[] nums) {
        int res = 0;
        for(int x:nums)
        {
            res ^= x;
        }
        return res;
    }
}
<----->TOPIC<----->
BIT MANIPULATION : XOR removes duplicates and keeps the lonely number.
